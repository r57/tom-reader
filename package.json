{
  "name": "tom-reader",
  "version": "0.0.3",
  "main": "src/index.ts",
  "license": "MIT",
  "devDependencies": {
    "@tsconfig/node16": "^16.1.0",
    "@types/node": "^20.4.6",
    "ts-node": "^10.9.1",
    "typescript": "^5.1.6"
  },
  "dependencies": {
    "axios": "^1.4.0",
    "mqtt": "^5.0.2"
  },
  "scripts": {
    "build": "tsc -p .",
    "start": "ts-node src/index.ts",
    "image": "docker build . -t \"dell.r57:32000/tom-reader:$(cat package.json | jq -r '.version')\"",
    "push": "docker push \"dell.r57:32000/tom-reader:$(cat package.json | jq -r '.version')\""
  }
}
